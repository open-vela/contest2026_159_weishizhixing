复制 `secrets.h.example` 为 `secrets.h`，填 WiFi 和电脑 IPv4。

Arduino：ESP32S3 Dev Module，USB CDC On Boot = Enabled，PSRAM = OPI PSRAM。含 OV2640 拍照与 ST7789 等级色。不需要 ArduinoJson。

与 `app/jianwei`、网页共用 `docs/protocol.md`。这是没有 openvela 工具链时的回退，不是第二套产品。
